version: '3.8'
services:
  pygame:
    image: pallini/pygame
    container_name: pygame
    build:
      context: .
    restart: always
    ports:
      - "5555:5555"
    volumes:
      - type: bind
        source: ./app
        target: /tmp/app
    tty: true
    #command: ["python3", "server.py"]